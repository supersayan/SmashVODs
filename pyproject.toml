[project]
name = "smashvods"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "notion-client>=3.0.0",
    "python-dotenv>=1.2.2",
    "python-youtube>=0.9.8",
]
